# Space Invaders (C++ / SDL2)

👾 A simple Space Invaders clone written in C++ using SDL2.  
The project uses Premake5 to generate Visual Studio project files.

## 📦 Requirements
- Windows
- Visual Studio 2022 or newer
- No SDL2 installation required (included in `vendor/SDL2`)

## 🛠️ How to Build
1. Clone the repository  
2. Open the `scripts/` folder  
3. Run `Build.bat`  
4. Open the generated `Space_Invaders.sln`  
5. Build and run from Visual Studio
